@model List<CIPlatform.Entities.ViewModels.VolunteerTimesheetRecordModel>
@if (Model.Count() == 0)
{
    <h3>No data found</h3>
}
else
{
    <table class="table p-3">
        <thead>
            <tr>
                <th>Mission</th>
                <th>Date</th>
                <th>Action</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var data in Model)
            {
                <tr>
                    <td class="goal-data-id d-none" id="goal-@data.TimesheetId"></td>
                    <td class="goal-mission" id="mission-@data.MissionId">@data.MissionName</td>
                    <td>@data.DateVolunteered.ToString().Substring(0,10)</td>
                    <td>@data.Action</td>
                    <td>
                        <img class="edit-goal-data" id="edit-@data.TimesheetId" style="cursor:pointer;" height="20px" src="~/images/Voted-Missions.png" alt="" data-bs-toggle="modal" data-bs-target="#goalTimesheetModal">
                    </td>
                    <td>
                        <img class="delete-goal-data" id="delete-@data.TimesheetId" style="cursor:pointer;"  src="~/images/bin.png" alt="">
                    </td>
                </tr>
            }
        </tbody>
    </table>
}